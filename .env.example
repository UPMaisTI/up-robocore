# Ambiente
NODE_ENV=development
PORT=3000
TZ=America/Sao_Paulo

# -----------------------------
# Oracle (opcional)
# Preencha DB_USER/DB_PASSWORD e um entre DB_CONNECT_STRING ou DB_SERVICE_NAME.
# Se deixar vazio, o app sobe sem DB.
# -----------------------------
DB_USER=
DB_PASSWORD=
DB_CONNECT_STRING=UPMAIS
# DB_SERVICE_NAME=UPMAIS

# Se usar alias do tnsnames.ora em desenvolvimento local, aponte o diretório:
# (No Docker do RoboCore, TNS_ADMIN já é /app/tns)
# TNS_ADMIN=/caminho/para/pasta/do/tns

# -----------------------------
# Pool de conexões (valores padrão)
# -----------------------------
DB_POOL_MIN=0
DB_POOL_MAX=4
DB_POOL_INC=1
DB_POOL_TIMEOUT=60
DB_QUEUE_TIMEOUT=120000

# -----------------------------
# Outros (exemplos)
# -----------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
FRONTEND_URL=http://localhost:3000

# -----------------------------
# WhatsApp Spam (robô whatsapp-spam)
# -----------------------------
# Base e chave da API de envio (wa)
WHATS_API_BASE=http://localhost:3000
WHATS_API_KEY=
# Re-scan de sessões (ms)
WHATS_RESCAN_MS=15000
# Texto da fazendinha (se vazio, usa versículo aleatório do DB)
WHATS_FARM_TEXT=
# Opção: sessões (ou NUM_ORIGEM) que buscam direto por NUMORIGEM no envia_whats
# Formatos: lista separada por vírgula
#  - WHATS_USE_ASSIGNED_SESSIONS=robot-1,robot-2
#  - WHATS_USE_ASSIGNED_NUMS=12,34
WHATS_USE_ASSIGNED_SESSIONS=
WHATS_USE_ASSIGNED_NUMS=

# -----------------------------
# Anexos (mapa de caminhos)
# -----------------------------
# Se definido, usa o compartilhamento SMB para ler anexos em DEV/local.
# Exemplo: //192.168.1.252/c$/
WHATS_WIN_SHARE_PREFIX=
